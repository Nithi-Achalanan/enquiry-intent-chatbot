# Required AI provider configuration. The application refuses to start when either is empty.
OPENAI_API_KEY=
PRIMARY_MODEL=gpt-4o-mini
# Optional: used when the primary model request fails.
FALLBACK_MODEL=gpt-4o-mini
